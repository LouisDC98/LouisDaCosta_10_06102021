import React from 'react';
import Header from '../../components/Header/Header';

function homePage() {
    return (
        <div>
            <Header sign={'in'} />
        </div>
    );
}

export default homePage;
